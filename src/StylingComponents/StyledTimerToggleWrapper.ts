import { styled } from 'styled-components';

export const StyledTimerToggleWrapper = styled.div`
    display: flex;
    justify-content: center;
    gap: 10px;
    padding: 20px;
    flex-direction: row;
`