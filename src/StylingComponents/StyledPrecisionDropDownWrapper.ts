import { styled } from 'styled-components';

export const StyledPrecisionDropDownWrapper = styled.div`
    display: flex;
    justify-content: center;
    gap: 5px;
    padding: 5px;
    flex-direction: column;
`