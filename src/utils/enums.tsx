// Enum to represent lap rating
export enum LapRating {
  Best = 'best',
  Worst = 'worst',
  Default = 'default',
}

// Enum to represent time units
export enum TimeUnit {
  Hours = 'hours',
  Minutes = 'minutes',
  Seconds = 'seconds',
  Centiseconds = 'centiseconds',
}
