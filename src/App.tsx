import React from 'react';
import StopWatch from './StopWatch'; // Adjust the path as needed to correctly import the StopWatch component

export default function App() {
    return (
        <div>
            <h1>Welcome to the Stopwatch App</h1>
            <StopWatch />
        </div>
    );
}