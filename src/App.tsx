import React from "react";
import Stopwatch from "./StopWatch";

const App: React.FC = () => {
    return (
        <div>
            <h1>Stopwatch App</h1>
            <Stopwatch />
        </div>
    );
};

export default App;