import React from 'react';
import StopWatch from './StopWatch';

// This is the main App component
export default function App() {
    // The App component renders the StopWatch component
    return (
        <div>
            <StopWatch />
        </div>
    );
}
