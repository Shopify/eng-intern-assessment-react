import React from 'react'
import StopWatch from './StopWatch';

// Create an App component that renders the StopWatch component
export default function App() {
    return(
        <div className="App">
            <StopWatch />
        </div>
    );
}