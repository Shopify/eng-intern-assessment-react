import React, {useState} from "react";

type LappedTimeProps = {
    lappedTime: number[];
};

export default function LappedTime (props:LappedTimeProps) {

    const {lappedTime} = props;
    // const {lapsRecorded, setLapsRecorded} = useState<boolean>(false)

    return (
        <>
        </>
    )
}