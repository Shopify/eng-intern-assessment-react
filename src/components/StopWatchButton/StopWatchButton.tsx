import React from "react";

export default function StopWatchButton() {
	//Start button starts counting
	// Click handler takes a function that:
	// sets startTime to systemTime (or date.now?)
	// Records date.now in a variable startTime
	// updates state??
	// Change button text to stop
	// undefined if the stopwatch is not started yet or is reset
	// TYPE: number | undefined

	// Stop button stops counting
	// Click handler stop counting (?) - stopsystemtime?
	// Store time stopped in a variable stopTime
	// Add time elapsed but subtracting startTime from Date.now()
	// Store result in lap array
	// Change button text to start

	// Reset button resets to zero
	// Reset counter to 0

	// Laps button records and displays laps
	// Display lap array

	return <div></div>;
}
